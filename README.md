⚠️  please only run the install script in the root of the repo directory

⚠️  the install script uses links so do not delete the repo

⚠️  pay attension to the last line outputed by the script

```sh
./install.sh
```
